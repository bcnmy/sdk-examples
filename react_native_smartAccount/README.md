### react-native-smartaccount

This repository demonstrates how to use the Biconomy SDK using ethers (private key) provider using react native.

```sh
npm i
npx react-native run-ios --simulator="iPhone 14"
```